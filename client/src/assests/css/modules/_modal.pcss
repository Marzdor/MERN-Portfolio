.modal {
  @mixin atSmall {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 99;
    background-color: #e2e7e9;

    display: flex;
    flex-direction: column;
  }
  &-form {
    @mixin atSmall {
      display: grid;
    }
  }
}
